# 📦 提交所有修改到 Git

## 提交所有修改的指令（在本地執行）

```powershell
# 進入專案目錄
cd phototype-ui

# 1. 查看所有修改的檔案
git status

# 2. 加入所有修改的檔案（不包括未追蹤的檔案）
git add -u

# 或者加入所有檔案（包括新檔案，但排除 .gitignore 中的檔案）
git add .

# 3. 提交所有修改
git commit -m "更新活動與評量設計功能及修復網絡圖跑版問題"

# 4. 推送到 GitHub
git push origin main
```

## 如果只想提交特定檔案（排除某些檔案）

```powershell
# 加入所有修改的檔案，但排除某些檔案
git add -u
git reset HEAD public/uploads/  # 排除上傳的檔案
git commit -m "更新活動與評量設計功能及修復網絡圖跑版問題"
git push origin main
```

## 在伺服器上更新

```bash
cd ~/projects/teacher-collaboration-system

# 清除快取
rm -rf .next

# 拉取最新代碼
git pull origin main

# 重新建置
npm run build

# 重啟服務
pm2 restart teacher-collab

# 查看狀態
pm2 status
```


